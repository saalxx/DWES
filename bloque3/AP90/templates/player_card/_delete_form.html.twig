<form method="post" action="{{ path('app_player_card_delete', {'id': player_card.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ player_card.id) }}">
    <button class="btn">Delete</button>
</form>
